node_modules
dist/
coverage/
.test-src/
*.tgz
.env
.env.*
.DS_Store
.pi/npm/
.pi/git/
# mmr-debug capture output (contains full prompt/session text; never commit)
.pi/mmr-debug/
*.mmr-capture.jsonl
docs/private/

# Local-only agent guidance (never publish). Pi auto-loads it alongside AGENTS.md.
CLAUDE.md
AGENTS.local.md
